language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t glaydson/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true glaydson/docker-react npm run test